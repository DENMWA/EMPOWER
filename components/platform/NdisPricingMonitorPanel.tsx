"use client";
import { useCallback, useEffect, useState } from "react";
import { ExternalLink, RefreshCw, ShieldCheck, UploadCloud } from "lucide-react";
import { Card, StatusBadge } from "@/components/ui";
import { getAuthenticatedApiHeaders } from "@/lib/supabase-auth";
type Version={id:string;version_name:string;effective_from:string;status:string}; type Diff={draft_pricing_version_id:string;new_items_count:number;removed_items_count:number;changed_price_count:number}; type State={monitor:any;versions:Version[];diffs:Diff[]};
export function NdisPricingMonitorPanel(){const[data,setData]=useState<State|null>(null);const[error,setError]=useState("");const[busy,setBusy]=useState("");const load=useCallback(async()=>{const r=await fetch("/api/platform/ndis-pricing",{headers:getAuthenticatedApiHeaders(),cache:"no-store"});const b=await r.json();if(!r.ok)throw new Error(b.error||"Pricing status could not be loaded.");setData(b)},[]);useEffect(()=>{void load().catch(e=>setError(e.message))},[load]);async function act(action:"check"|"publish",versionId?:string){setBusy(action);setError("");try{const r=await fetch("/api/platform/ndis-pricing",{method:"POST",headers:{...getAuthenticatedApiHeaders(),"Content-Type":"application/json"},body:JSON.stringify({action,versionId})});const b=await r.json();if(!r.ok)throw new Error(b.error||"Pricing action failed.");await load()}catch(e){setError(e instanceof Error?e.message:"Pricing action failed.")}finally{setBusy("")}}const draft=data?.versions.find(v=>v.id===data.monitor?.draft_version_id&&v.status==="draft");const active=data?.versions.find(v=>v.status==="active");const diff=data?.diffs.find(d=>d.draft_pricing_version_id===draft?.id);return <Card className="border-teal-200"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase text-teal-700">Controlled pricing source</p><h2 className="mt-2 text-xl font-bold text-ink">Official NDIS pricing</h2><p className="mt-2 text-sm text-slate-600">Automatic checks create drafts. Only your approval can publish prices.</p></div><div className="flex items-center gap-2"><StatusBadge label={data?.monitor?.status?.replaceAll("_"," ")||"Not checked"} tone={draft?"amber":active?"green":"blue"}/><button type="button" onClick={()=>void act("check")} disabled={!!busy} className="inline-flex min-h-10 items-center gap-2 rounded-md border border-slate-300 bg-white px-3 text-sm font-semibold text-ink hover:border-teal-400 disabled:opacity-50"><RefreshCw size={16} className={busy==="check"?"animate-spin":""}/>{busy==="check"?"Checking...":"Check now"}</button></div></div>{error?<p className="mt-4 rounded-md bg-red-50 p-3 text-sm font-semibold text-red-800">{error}</p>:null}<div className="mt-5 grid gap-4 lg:grid-cols-2"><div className="rounded-md border border-slate-200 bg-slate-50 p-4"><div className="flex items-center gap-2"><ShieldCheck size={18} className="text-emerald-700"/><h3 className="font-bold text-ink">Live version</h3></div><p className="mt-3 font-semibold text-ink">{active?.version_name||"No platform version published"}</p><p className="mt-1 text-sm text-slate-600">{active?`Effective ${active.effective_from}`:"Provider catalogues remain unchanged."}</p></div><div className="rounded-md border border-slate-200 bg-white p-4"><div className="flex items-center gap-2"><UploadCloud size={18} className="text-teal-700"/><h3 className="font-bold text-ink">Review queue</h3></div><p className="mt-3 font-semibold text-ink">{draft?.version_name||data?.monitor?.detected_filename||"No draft awaiting review"}</p><p className="mt-1 text-sm leading-6 text-slate-600">{data?.monitor?.detail||"Run the first official source check."}</p>{diff?<div className="mt-3 flex gap-3 text-xs font-semibold text-slate-700"><span>{diff.changed_price_count} price changes</span><span>{diff.new_items_count} new</span><span>{diff.removed_items_count} removed</span></div>:null}{data?.monitor?.detected_download_url?<a href={data.monitor.detected_download_url} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-sm font-semibold text-teal-800">Open official file <ExternalLink size={14}/></a>:null}{draft?<button type="button" onClick={()=>void act("publish",draft.id)} disabled={!!busy} className="mt-4 inline-flex min-h-10 w-full items-center justify-center rounded-md bg-teal-800 px-4 text-sm font-bold text-white hover:bg-teal-900 disabled:opacity-50">{busy==="publish"?"Publishing...":"Publish reviewed version"}</button>:null}</div></div>{data?.monitor?.last_checked_at?<p className="mt-4 text-xs text-slate-500">Last checked {new Date(data.monitor.last_checked_at).toLocaleString("en-AU")}</p>:null}</Card>}
