import { getCurrentOrganisationId, getCurrentUserId, supabaseRequest } from "@/lib/supabase-rest";
export type HandoverEntry = { id: string; houseId: string; participantId: string; category: string; priority: "routine" | "important" | "urgent"; summary: string; followUpAction: string; sourceType: string; sourceId: string; createdAt: string; acknowledged: boolean };
type Row = { id:string; house_id:string; participant_id:string|null; category:string; priority:HandoverEntry["priority"]; summary:string; follow_up_action:string|null; source_type:string|null; source_id:string|null; created_at:string };
export async function getRecentHandovers(hours = 24) { const since = new Date(Date.now() - hours * 3600000).toISOString(); const [items, read] = await Promise.all([supabaseRequest<Row[]>("handover_entries", { query:`select=id,house_id,participant_id,category,priority,summary,follow_up_action,source_type,source_id,created_at&created_at=gte.${encodeURIComponent(since)}&order=created_at.desc` }), supabaseRequest<Array<{handover_entry_id:string}>>("handover_acknowledgements", { query:"select=handover_entry_id" })]); const ids = new Set((read.data||[]).map(x=>x.handover_entry_id)); return (items.data||[]).map((x):HandoverEntry=>({id:x.id,houseId:x.house_id,participantId:x.participant_id||"",category:x.category,priority:x.priority,summary:x.summary,followUpAction:x.follow_up_action||"",sourceType:x.source_type||"",sourceId:x.source_id||"",createdAt:x.created_at,acknowledged:ids.has(x.id)})); }
export async function createHandoverEntry(input: Omit<HandoverEntry,"id"|"sourceType"|"sourceId"|"createdAt"|"acknowledged">) { const organisationId=await getCurrentOrganisationId(), userId=getCurrentUserId(); if(!organisationId||!userId)return{saved:false,error:"Sign in before saving handover information."}; const r=await supabaseRequest<Array<{id:string}>>("handover_entries",{method:"POST",body:{organisation_id:organisationId,house_id:input.houseId,participant_id:input.participantId||null,category:input.category,priority:input.priority,summary:input.summary,follow_up_action:input.followUpAction||null,created_by:userId}}); return{saved:Boolean(r.data?.length&&!r.error),error:r.error}; }
export async function acknowledgeHandover(entry:HandoverEntry){const organisationId=await getCurrentOrganisationId(),userId=getCurrentUserId();if(!organisationId||!userId)return{saved:false,error:"Sign in before acknowledging handover."};const r=await supabaseRequest<Array<{handover_entry_id:string}>>("handover_acknowledgements",{method:"POST",query:"on_conflict=handover_entry_id,user_id",prefer:"resolution=merge-duplicates,return=representation",body:{handover_entry_id:entry.id,organisation_id:organisationId,user_id:userId}});return{saved:Boolean(r.data?.length&&!r.error),error:r.error};}
