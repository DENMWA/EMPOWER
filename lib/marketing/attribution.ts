import type { Attribution, SourceClass } from "@/lib/marketing/types";
const searchHosts=["google.","bing.com","duckduckgo.com","yahoo."];
export function classifyMarketingSource(input:Pick<Attribution,"oppref"|"gclid"|"utmSource"|"utmMedium"|"referrer">):SourceClass{const source=input.utmSource.toLowerCase(),medium=input.utmMedium.toLowerCase();if(input.oppref||["chatgpt","openai"].includes(source))return "openai_ads";if(input.gclid||(source==="google"&&/cpc|ppc|paid/.test(medium)))return "google_ads";if(/cpc|ppc|paid|display|social/.test(medium))return "other_paid";if(input.referrer){try{const host=new URL(input.referrer).hostname.toLowerCase();if(searchHosts.some(x=>host.includes(x)))return "organic_search";return "referral"}catch{return "other"}}return "direct"}
export function sanitiseAttribution(input:Record<string,unknown>):Attribution{const text=(key:string,max=240)=>typeof input[key]==="string"?String(input[key]).slice(0,max):"";const value={utmSource:text("utmSource",100),utmMedium:text("utmMedium",100),utmCampaign:text("utmCampaign",160),utmContent:text("utmContent",160),utmTerm:text("utmTerm",160),gclid:text("gclid",240),oppref:text("oppref",240),referrer:text("referrer",500),landingPath:safePath(text("landingPath",300)),sourceClass:"direct" as SourceClass};value.sourceClass=classifyMarketingSource(value);return value}
export function safePath(value:string){if(!value.startsWith("/")||value.startsWith("//"))return "/";return value.split("?")[0].slice(0,240)}
